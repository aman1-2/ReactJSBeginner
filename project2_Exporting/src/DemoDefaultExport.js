import React from "react"

const DefaultAlias = () => {
    return(
        <div>
            <h1>This is Default Export but at the import will give alias name.</h1>
        </div>
    );
}

export default DefaultAlias;